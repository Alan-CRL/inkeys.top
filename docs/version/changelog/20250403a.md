---
title: 20250403a 新版本发布
sticky: false
tags:
  - 版本
---

Inkeys20250301a #52 
---
`+` 新增创建单次快捷方式按钮
`+` 将选项界面加入置顶序
`+` 为自动更新增添进度显示和更多提示
`+` DDB增加更多精细控制
`=` 优化插件选项界面布局
`=` 选项界面更宽的滚动条
`=` 主栏的自适应的拖动
`=` 启动时遇到错误可以尝试重启一次再报错
`-` 修复了旧版本升级过程中重复下载无法自动更新的问题
`-` 修复窗口创建后意外地获取到焦点的问题
`-` 修复 Ppt 结束放映后小概率光标消失的问题
`-` 修复 Ppt 结束放映确认窗口弹出时无法操控键盘的问题
`-` 修复 Ppt 末页绘制时可能无法帮助退出 Ppt 的问题
`-` 修复 Ppt 动画加载过程中按 esc 退出也会导致鼠标消失的问题
`-` 修复定格时不会排除选项窗口的问题
`-` 修复快捷方式保障助手意外删除桌面快捷方式的问题
`-` 修复定格模式下窗口仍然会穿透窗口的问题

Inkeys20250309a #58  
---
`=` 优化窗口置顶逻辑
`=` 优化窗口消息传递逻辑
`=` 优化关闭自动更新选项后的逻辑
`-` 修复选项->插件页面无法下滑的问题
`-` 修复选项->绘制->智能绘图中拉直选项无法单独开关的问题

Inkeys20250403a #61 
---
作为预备正式版本，没有任何更新说明

---

`!` 注意：这是个网络发布版本，LTS通道将于杀软人工审核通过后与此版本同步