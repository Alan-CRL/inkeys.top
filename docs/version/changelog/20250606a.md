---
title: 20250606a 新版本发布
sticky: false
tags:
  - 版本
---

Inkeys20250606a #89 
(CI/CD：<https://github.com/Alan-CRL/Inkeys/actions/runs/15494115557>)
---
`+` 新增：使用 CI/CD 构建版本
`+` 新增：CI/CD 详细信息现在可以在Actions任务页面、发布Package中、发布版本的Tips.txt中和软件内的软件版本中查看
`+` 新增：使用 SHA256 代码签名
`+` 新增：超级置顶插件
`+` 新增：选项新增教学安全选项
`+` 新增：选项新增显示ppt加载动画选项
`+` 新增：选项新增保存板块
`+` 新增：启用配置清理的设置选项
`+` 新增：修复时修正软件架构选项
`+` 新增：在自动更新失败时出现手动更新链接
`+` 新增：选项预设选项卡
`+ ****` 记忆绘制颜色
`+ ****` 记忆绘制粗细
`+ ****` 自适应绘制粗细
`+ ****` 预设默认画笔粗细
`+ ****` 预设默认荧光笔粗细
`=` 修正：系统架构不一致时的提示
`=` 修正：更新通道提示内容
`=` 优化：绘制时光标隐藏的效果
`=` 优化：Ppt联动未勾选记忆控件位置时，将在退出ppt恢复控件位置
`=` 优化：为选项中的拖动条增添了数值反馈
`=` 优化：绘制粗细预设
`-` 缓解：Ppt联动模块导致关闭程序时出现的程序运行错误问题

---

**Full Changelog**: <https://github.com/Alan-CRL/Inkeys/compare/20250503a...20250606a>

---

`!` 注意：这是发布的 正式版本，更新通道（正式通道）中的软件版本将于杀软人工审核通过后与此版本同步