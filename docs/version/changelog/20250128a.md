---
title: 20250128a 新版本发布
sticky: false
tags:
  - 版本
---

[Inkeys20250128a](https://github.com/Alan-CRL/IDT/commit/04a3b368c6a571ad2bad246a49fde6a158878903)  
---
`+` 全新 设置3.0 界面  
`+` 新 智绘教Inkeys 图标  
`+` 全程序 Unicode 编码化  
`+` 向下兼容至 Windows 7 sp0  
`+` 全新 更新系统3.0 与程序更新 https 加密下载  
`+` 新增 快捷方式保障助手 插件  
`+` 智能绘图新增直线模式下的端点吸附  
`+` 新增重复开启程序提示  
`+` 新增运行程序强制置顶选项  
`+` 细化智能绘图选项，可单独控制抬笔拉直，停留拉直，端点吸附  
`+` 新增预览体验计划模块，申请通过的成员用户可以切换通道到预览通道  
`+` 新增程序修复和预览成员的版本回退功能  
`+` 新增全新问题意见反馈通道  
`+` 自动更新新增更多更新信息反馈  
`+` 修复绘图设备被错误识别的问题  
`+` 新增落笔预备功能  
`+` 新增压感橡皮功能  
`+` PPT控件支持全区域拖动  
`+` PPT控件结束按钮改为抬起触发，并增加拖动功能  
`+` 对 ppt 翻页的 enter 和 backspace 的支持  
`+` 优化点击主栏时误识别为拖动，而无法展开的问题  
`=` 降低 CPU 占用比率  
`=` 降低内存占用比率  
`=` 开机启动设置更简便  
`=` 优化停留拉直的识别准确度  
`=` 优化抬笔拉直的识别准确度  
`=` 优化直线/矩形吸附的准确度  
`=` 去除 CrashedHandler 辅助程序，主程序已经相对稳定  
`=` ImGui dx11 转为使用 dx9，增强系统兼容性  
`=` 程序将从内存中正确加载字体，不依赖本地字体文件  
`=` 提升开机启动优先级  
`=` 画笔粗细选择将随着屏幕大小变化  
`=` 插件设置中增加了一些实用性提示  
`=` 触控库重构  
`=` 用户ID获取方式改用新版，减小冲突  
`=` 修复启动时会错误地抢占焦点的问题  
`=` 龙年迎新皮肤替换为蛇年迎新  
`-` 修复 WPS 放映检测不及时或无法检测到的问题  
`-` 修复快速开关悬浮窗拦截插件会导致插件失效的问题  
`-` 修复 ppt 两侧控件在末页时书写时退出放映是失效的  
`-` 修复图形颜色和粗细在抬笔时错误变化的问题  
`-` 修复了由于引用程序名称包含非ASCII字符导致DWN错误导致应用程序无法启动的问题  
`-` 修复了绘制矩形时点击导致的绘制异常问题  
`-` 修复了 PPT 中部控件缩放无法正确保存的问题  
`-` 修复了通过快捷键切换到画笔时画笔选项栏不会收回的问题  
`-` 修复了通过任务栏关闭选项页面后无法再次打开选项的问题  
`-` 修复在结束放映页时PPT控件下一页按钮图标显示错误的问题  
`-` 修复由于颜色的自动切换，悬浮窗背景颜色会无法切换的问题  

`!` 注意：这是个网络发布版本，LTS通道将于杀软人工审核通过后与此版本同步