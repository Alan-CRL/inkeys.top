---
title: 20250721a 新版本发布
sticky: false
tags:
  - 版本
---

Inkeys20250721a(LTS) #96 
(CI/CD：<https://github.com/Alan-CRL/Inkeys/actions/runs/16408242422>)
---
`+` 新增：语言 `English(BETA)`，`繁體中文(BETA)`
`=` 优化：选项界面显示效果（下拉框）
`=` 优化：启动命令行优化，重构超级置顶逻辑
`=` 优化：社区名片界面
`=` 优化：去除自动关闭游离卡死的wpp选项，优化代码后wpp现在已经可以正常退出
`-` 修复：使用穿透后可能出现的卡顿问题
`-` 修复：ppt 意外结束放映时导致画板状态出现错误的问题
`-` 修复：ppt 绘制下，在末页无法拦住并提示的问题
`-` 修复：提示更新到最新版本时，如果修复并选择了不一样的架构，错误安装不同架构的问题
`-` 修复：可能无法普通置顶的问题

---

**Full Changelog**: <https://github.com/Alan-CRL/Inkeys/compare/20250719a...20250721a>

---

`!` 注意：这是发布的 正式版本，更新通道（正式通道）中的软件版本将于杀软人工审核通过后与此版本同步