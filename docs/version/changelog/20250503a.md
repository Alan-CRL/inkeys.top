---
title: 20250503a 新版本发布
sticky: false
tags:
  - 版本
---

Inkeys20250503a #64 
---
`+` 在绘制窗口中会禁用相关触摸手势（边缘手势，长按手势和笔势），使书写更流畅
`+` 选项界面支持按住滑动翻页
`+` 新增ppt控件缩放同步调节
`+` 新增高性能绘图性能选项
`+` 新增隐藏触摸光标选项
`+` 新增“拖动主栏时收起主栏”和“点击时收起主栏”选项
`+` 新增更新通道提示
`+` 新增更新架构选择
`+` 新增更多实用组件
`+` 选项新增教学安全选项
`+` 选项新增显示ppt加载动画选项
`+` 选项新增保存板块
`=` 当自动更新失败时则会继续运行旧版本
`-` 选色盘意外导致程序崩溃问题
`-` 修复低性能机器上易触发的绘制不跟手及卡顿问题
`-` 修复撤回和超级恢复导致的崩溃问题

---

**Full Changelog**: <https://github.com/Alan-CRL/Inkeys/compare/20250301a...20250503a>

---

`!` 注意：这是发布的 正式版本，更新通道（正式通道）中的软件版本将于杀软人工审核通过后与此版本同步